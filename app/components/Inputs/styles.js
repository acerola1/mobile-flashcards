import EStyleSheet from 'react-native-extended-stylesheet';

export default EStyleSheet.create({
  textInput: {
    fontSize: 25,
    color: '$white',
    margin: 20,
    borderWidth: 1,
    borderColor: '$white',
    borderRadius: 5,
    padding: 10,
  },
});
